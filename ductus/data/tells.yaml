# The tells catalogue: patterns that make prose read as machine-written.
#
# Deterministic, auditable, and deliberately incomplete -- a regular expression sees a
# phrase, never a shape, so this file is the floor of what `ductus` can find and never
# the ceiling. The rhetoric and rhythm detectors, and any LLM judge layered on top,
# cover what patterns cannot.
#
# A rule has two axes, and they answer different questions. Keeping them apart is what
# lets measurement move one without disturbing the other.
#
# `tier` is how firmly a rule is worth enforcing *as writing advice*. `acquaint` reads
# it and never reads `weight`: a reader who tolerates AI-sounding prose is shown tier E
# only, a neutral reader E and W, an averse reader all three.
#   E  worth fixing whoever is reading
#   W  worth fixing unless it is the author's own habit
#   S  common in careful human writing too -- fix when it clusters, or for an averse reader
#
# `weight` (optional, per rule) is how much a hit is worth as *evidence about who wrote
# the text*. `ductus` reads it; `acquaint` never does. Absent, it falls back to the
# tier's default. A rule can be firm advice and near-worthless evidence at the same
# time, and `summary-closer` below is exactly that.
#
# These tiers originally read as confidence that a model wrote it -- E meaning
# "near-certain artifact". Measuring 350 human-written documents falsified that for
# several rules while leaving them worth fixing as advice, which is why the axes were
# split rather than the rules demoted. See misc/docs/reducing-false-accusations.md.
#
# Ordinary human courtesy ("I hope this finds you well", "happy to help") is S at most.
#
# Patterns are case-insensitive, multi-line regular expressions. `.` stands in for an
# apostrophe so straight and curly quotes both match. Examples are fictional.
#
# History: this catalogue began in `acquaint` (whose `deslop` needed the write-side
# half) and moved here so the read-side and the write-side share one source of truth.
# `acquaint` now reads it from this package; its recipient-calibration layer -- whose
# tells are enforced for which reader, and the per-reader blocklist -- stays there.
#
# Lineage of the tiering logic: the Vale `signs-of-ai-writing` style pack.

metrics:
  # Rhythm is only judged on text long enough to have one.
  min_sentences_for_rhythm: 6
  sentence_len_cv_min: 0.35
  # Headers and bold in a message this short are a formatting tell.
  short_message_words: 150

rules:
  - id: chat-leftover
    tier: E
    message: assistant chatter left in the draft
    patterns:
      - '\b(certainly!|great question|i hope this helps|as an ai\b)'

  - id: unfilled-placeholder
    tier: E
    message: a placeholder is still unfilled
    patterns:
      - '\[ASK:[^\]]*\]'
      - '\[(insert|your name|recipient|company name)[^\]]*\]'
      - '\{\{[^}]+\}\}'

  - id: cutoff-disclaimer
    tier: E
    message: a model disclaimer leaked into the draft
    patterns:
      - '\bas of my (last|knowledge) (update|cutoff)'
      - '\bi (do not|don.t) have (access to )?real-time'

  - id: throat-clearing
    tier: E
    message: throat-clearing; delete it and keep the point
    patterns:
      - '\bit.s (important|worth) (to note|noting)\b'

  - id: summary-closer
    tier: E
    # Tier E for `acquaint`: as style advice, "In conclusion," is worth flagging even
    # for a reader who tolerates AI-sounding prose. Weight 0.15 for `ductus`: measured
    # against 350 human-written texts it implicated 25 of them and matched machine
    # ground truth zero times, so as evidence about *authorship* it is worth almost
    # nothing. Tier and weight answer different questions. See
    # misc/docs/reducing-false-accusations.md.
    weight: 0.15
    message: a closer that restates the message; end on the last real point
    patterns:
      - '\b(in conclusion|to sum up|in summary),'

  - id: ai-vocabulary
    tier: W
    message: vocabulary models overuse; say the plain thing
    patterns:
      - '\b(delve[sd]?|delving|tapestry|realm|robust|seamless(ly)?|leverag(e|es|ed|ing)|vibrant|pivotal|testament to|boasts?|fosters?|underscores?|showcas(e|es|ed|ing))\b'
      - '\b(elevate[sd]?|empower(s|ed|ing)?|unlock(s|ed|ing)?|unleash(es|ed|ing)?|game-changer|cutting-edge|intricate|meticulous(ly)?)\b'
      - '\bin today.s (fast-paced|digital|ever-evolving) world\b'
      - '\b(dive deep|deep dive|at its core|the beauty of|navigate the complexities)\b'
      - '\bplays? an? (vital|key|crucial|pivotal) role\b'

  - id: filler-adverb
    tier: W
    message: a filler adverb opening a sentence; start with the point
    patterns:
      - '(^|[.!?]\s+)(notably|interestingly|essentially|in essence),'

  - id: empty-transition
    tier: W
    message: an empty transition; let the sentences connect on their own
    patterns:
      - '(^|[.!?]\s+)(moreover|furthermore|additionally),'

  - id: copula-avoidance
    tier: W
    message: '"serves as" / "stands as": say "is"'
    patterns:
      - '\b(serves|stands) as (a|an|the)\b'

  - id: contrastive-negation
    tier: W
    count_against: contrastive_max
    message: the "not X, but Y" construction; state Y
    patterns:
      # `only` was removed from both patterns: "not only X but (also) Y" is the
      # ordinary English correlative, not the model's antithesis habit. It matched 19
      # human-written documents and zero machine-written spans. Telling a writer to
      # remove it is bad advice too, so this narrowing is right for `acquaint` as well.
      - '\b(is|are|was|were|it.s|this is|that.s)(n.t| not) (just|merely|simply|about)\b[^.!?\n]{0,80}?(\bit.s\b|\bbut\b|—)'
      - '\bnot (just|merely|simply) [^.!?\n]{1,60}?,? but\b'

  - id: ing-trailer
    tier: W
    message: a trailing "-ing" clause that adds commentary, not content
    patterns:
      - ',\s+(highlighting|showcasing|underscoring|emphasi[sz]ing|demonstrating|reflecting|making it)\b[^.!?\n]*[.!?]'

  - id: significance-inflation
    tier: W
    message: significance inflation; show the thing instead
    patterns:
      - '\b(game[- ]changing|groundbreaking|revolutionary|transformative|a new era)\b'

  - id: vague-attribution
    tier: W
    message: a claim attributed to nobody; cite the source or drop it
    patterns:
      - '\b(experts (say|agree|believe)|studies (show|suggest)|research (shows|suggests)|many (believe|say))\b'

  - id: sycophancy
    tier: W
    message: praise of the reader or their idea
    patterns:
      - '\b(what a (great|fantastic|wonderful)|brilliant (idea|question|point)|you.re absolutely right)\b'

  - id: stock-pleasantry
    tier: S
    message: a stock pleasantry; fine from a person, a tell when it pads a generated draft
    patterns:
      - '\bi hope (this|my) (email |message |note )?finds you well\b'
      - '\b(happy to help|let me know if you.d like|feel free to reach out|don.t hesitate to reach out)\b'

  - id: rhetorical-opener
    tier: S
    message: a rhetorical question as an opener
    patterns:
      - '\A\s*(what if|have you ever wondered|imagine)\b[^?\n]*\?'

  - id: hedge-stack
    tier: S
    message: stacked hedges; keep one, and make it specific
    patterns:
      - '\b(could|may|might) (potentially|possibly|perhaps)\b'
      - '\bit (seems|appears) that it (might|may|could)\b'

  - id: triad
    tier: S
    count_against: triads_max
    message: lists of three everywhere; break some into twos and fours
    patterns:
      - '\b[\w-]+, [\w-]+,? and [\w-]+\b'

  - id: exclamation
    tier: S
    message: an exclamation mark in professional prose
    patterns:
      - '!(?=\s|$)'

# Messages the operator writes themself: condolence, apology for real harm, conflict,
# performance feedback. Routine apologies ("sorry for the delay", "my apologies, wrong
# file attached", "I apologize for any inconvenience", "sorry, I can't make Friday") are not.
